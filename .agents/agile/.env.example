# Artemis Configuration
# Copy this file to .env and fill in your API keys

# ============================================================================
# LLM Provider Configuration
# ============================================================================

# LLM Provider: "openai" or "anthropic"
ARTEMIS_LLM_PROVIDER=openai

# OpenAI API Key (required if using openai)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (required if using anthropic)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# ============================================================================
# Optional Configuration
# ============================================================================

# Specific model to use (optional, defaults to provider's best model)
# OpenAI models: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4
# Anthropic models: claude-sonnet-4-5-20250929, claude-3-5-sonnet-20241022
# ARTEMIS_LLM_MODEL=gpt-4o

# RAG Database Path (optional, defaults to /tmp/rag_db)
# ARTEMIS_RAG_DB_PATH=/tmp/rag_db

# Enable verbose logging (optional, defaults to true)
# ARTEMIS_VERBOSE=true

# ============================================================================
# Redis Configuration (Optional - for caching and real-time features)
# ============================================================================

# Redis Host (default: localhost)
REDIS_HOST=localhost

# Redis Port (default: 6379)
REDIS_PORT=6379

# Redis Database Number (default: 0)
REDIS_DB=0

# Redis Password (optional - leave commented if no password)
# REDIS_PASSWORD=your-redis-password

# Redis Connection Timeout in seconds (default: 5)
# REDIS_TIMEOUT=5
# REDIS_CONNECT_TIMEOUT=5
